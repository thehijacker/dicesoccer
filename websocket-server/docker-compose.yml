version: '3.8'

services:
  websocket-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: dicesoccer-websocket-server:latest
    container_name: dicesoccer-ws
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
      - CORS_ORIGIN=*
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:7860', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - dicesoccer-network

networks:
  dicesoccer-network:
    driver: bridge
